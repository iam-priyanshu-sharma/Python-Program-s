# Declare a list
my_list = [1, 2, 3, 5]

# Specified index and element to add
index_to_add = 3
element_to_add = 4

# Adding the element at the specified index
my_list.insert(index_to_add, element_to_add)

# Print the updated list
print("List after adding the element:", my_list)
